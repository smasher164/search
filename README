search

CLI tool to open browser tab and search under a specific site.

Usage:
  search [--google] [<query> ...]
  search [--hn] [<query> ...]
  search [--reddit] [<query> ...]
  search [--nix] [<query> ...]
  search [--scihub] [<query> ...]
